# MiniDB

MiniDB is a small prototype database engine written in C++.  
It lets you create tables at runtime and run basic SQL‑like commands from an interactive shell.

---

## How to run
Compile with g++:

```bash
g++ -std=c++17 db.cpp -o minidb
